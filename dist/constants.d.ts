export declare const JFK_IATA = "JFK";
export declare const AVIATIONSTACK_BASE = "http://api.aviationstack.com/v1";
/** Max characters a tool response may contain before truncation. */
export declare const CHARACTER_LIMIT = 8000;
/** Default page size when caller does not specify a limit. */
export declare const DEFAULT_LIMIT = 10;
/** Hard cap so callers cannot request enormous payloads. */
export declare const MAX_LIMIT = 50;
/** HTTP request timeout in milliseconds. */
export declare const REQUEST_TIMEOUT_MS = 10000;
//# sourceMappingURL=constants.d.ts.map